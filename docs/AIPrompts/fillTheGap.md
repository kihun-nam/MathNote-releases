Fill in the missing detail in the attached note across two sections: skipped proof steps, and any margin questions the author addressed to themselves.

Read the source AND any attached photos AND any reference PDFs deeply enough to understand the substance — what's being proved, what techniques are in play, what results are being appealed to. Photos may carry handwritten gap markers (arrows between equations, "TODO", "WHY?", circled steps with question marks) that aren't visible in source; treat them as first-class gaps. Reference PDFs in the bundle may contain the exact missing argument the author elided ("by [Smith]" with Smith bundled as a PDF) — consult them before deriving from scratch. If a User Background page is present, read it too; the user's notation conventions and preferred references inform how the gap-filling should look.

Section 1 — Proof gap-filling.
Read the note end to end and identify every place where a step is sketched, abbreviated, or skipped — phrases like "similarly", "an easy computation gives", "by the same argument", "it follows that", unjustified passages from one line of an aligned derivation to the next, applications of named theorems whose hypotheses have not been checked, and any step the author flagged with `[GAP]`, `[?]`, or similar. Include photo-borne gap markers (arrows, TODOs, question marks in handwritten content). For each gap, supply the missing argument in full. Label each insertion with [GAP-N] in sequence (GAP-1, GAP-2, ...) and wrap multi-line insertions in <!-- INS START --> ... <!-- INS END --> blocks. Preserve the surrounding source verbatim; insertions go inline at the location of the gap.

When filling a gap by appealing to a named theorem (Cauchy–Schwarz, Lebesgue dominated convergence, Banach fixed-point, Hahn–Banach, etc.), state the theorem explicitly and verify its hypotheses are met by the note's setup. If you cannot verify a hypothesis, flag with [! ...] inline rather than asserting the appeal works. Do not bias toward recent named results — classical theorems are the most common gap-fillers and should be reached for first. If a referenced result is bundled as a reference PDF, cite it inline ("by [Smith Theorem 3.1, see attached PDF]") rather than re-deriving.

If you genuinely cannot fill a gap because the surrounding context is too sparse, say so explicitly with a [! GAP-N: cannot fill — needs [missing context]] inline marker rather than fabricating an argument. If the note has no gaps (every step is fully justified), return the source unchanged and add a single sentence at the end of Section 1: "No gaps detected." Do not invent gaps to pad the section.

Section 2 — Author's margin questions.
Find any question the author wrote to themselves in the note (source AND photos) — e.g., "why does this work?", "is this tight?", "check this", "verify", "is the constant sharp?", "does this need [hypothesis]?". Answer each one directly using the bundle context: source, photos, reference PDFs, and User Background if present. One question, one answer, short. Do not fabricate questions; if there are none, say so and skip this section. Format each as a "### Question N" subsection containing the verbatim question (in quotes) followed by the answer.

If a margin question's answer requires invoking a named theorem or reference, cite inline using the same convention as Section 1. If you cannot answer confidently from the bundle, flag uncertainty in the answer with [! ...] rather than speculating.
